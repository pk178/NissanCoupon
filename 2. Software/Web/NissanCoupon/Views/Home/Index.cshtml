@{
    ViewBag.Title = "KiemTraCoupon";
}

<div class="container" ng-controller="homeController" style="margin-top:250px;">
    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">{{'KiemTraCoupon'|translate}}</h3>
                </div>
                <div class="panel-body" ng-show="!valid && !confirm && !success">
                    <form method="post" name="formCheck">
                        <fieldset class="text-center">
                            <p ng-if="message" class="text-danger">{{message|translate}}</p>
                            <div class="form-group">
                                <input class="form-control" placeholder="{{'MaCoupon'|translate}}" ng-model="code" type="text" required autocomplete="off" autofocus>
                            </div>
                            <button type="submit" ng-click="check()" class="btn btn-info">{{'KiemTra'|translate}}</button>
                        </fieldset>
                    </form>
                </div>
                <div class="panel-body" ng-show="valid && !confirm && !success">
                    <form method="post" name="formConfirm">
                        <fieldset class="text-center">
                            <p ng-if="message" class="text-danger">{{message|translate}}</p>
                            <p class="text-center">
                                Mã {{coupon.GiftCode}} tương ứng với {{coupon.EntitledServiceGift}} sử dụng cho xe {{coupon.LicensePlateNumber}}, hạn sử dụng {{coupon.ExpriedDate}}.
                                Bạn có muốn sử dụng mã quà tặng/ khuyến mại này hay không?
                            </p>
                            <button ng-click="yes()" class="btn btn-info">{{'Co'|translate}}</button>
                            <button ng-click="no()" class="btn btn-default">{{'Khong'|translate}}</button>
                        </fieldset>
                    </form>
                </div>
                <div class="panel-body" ng-show="valid && confirm && !success">
                    <form method="post" name="formOtp">
                        <fieldset class="text-center">
                            <p ng-if="message" class="text-info">{{message|translate}}</p>
                            <p>Hệ thống đã gửi mã xác nhận tới số điện thoại đăng ký của khách hàng. Vui lòng nhập mã xác nhận</p>
                            <div class="form-group">
                                <input class="form-control" placeholder="{{'MaOtp'|translate}}" ng-model="otp" type="text" required autocomplete="off" autofocus>
                            </div>
                            <button type="submit" ng-click="use()" class="btn btn-info">{{'XacNhan'|translate}}</button>
                        </fieldset>
                    </form>
                </div>
                <div class="panel-body" ng-show="valid && confirm && success">
                    <form method="post" name="formOtp">
                        <fieldset class="text-center">
                            <p ng-if="message" class="text-info">{{message|translate}}</p>
                            <button type="submit" ng-click="no()" class="btn btn-info">{{'OK'|translate}}</button>
                        </fieldset>
                    </form>
                </div>
            </div>
            @*<div ng-show="valid && !confirm && !success">
                <p></p>
                <p><b>Thông tin coupon:</b></p>
                <p>
                    {{'CustomerName'|translate}}: {{coupon.CustomerName}}
                    <br />
                    {{'PhoneNumber'|translate}}: {{coupon.PhoneNumber}}
                    <br />
                    {{'VehicleModel'|translate}}: {{coupon.VehicleModel}}
                    <br>
                    {{'ChassisNumber'|translate}}: {{coupon.ChassisNumber}}
                    <br />
                    {{'LicensePlateNumber'|translate}}: {{coupon.LicensePlateNumber}}
                    <br />
                    {{'GiftCode'|translate}}: {{coupon.GiftCode}}
                    <br />
                    {{'ExpriedDate'|translate}}: {{coupon.ExpriedDate}}
                    <br />
                    {{'EntitledServiceGift'|translate}}: {{coupon.EntitledServiceGift}}
                    @*<br />
                    {{'RedeemedDate'|translate}}: {{coupon.RedeemedDate}}
                    <br />
                    {{'RedeemedByDealer'|translate}}: {{coupon.RedeemedByDealer}}
            </p>
        </div>*@
    </div>
</div>
</div>
